@using HST.Art.Web
@using HST.Art.Core

@model List<CategoryDictionary>
@if (Model != null && Model.Count > 0)
{
    <ul class="menu">
        @foreach (CategoryDictionary item in Model)
        {
            <li class=""><a href="#">@item.Name</a></li>
        }
    </ul>
}

